# Set up and Run

1. Install `trunk` && `tauri`

    ```
    cargo install trunk tauri-cli
    ```

2. run

    - dev

    ``` bash
    cargo tauri dev
    ```

    - build

    ``` bash
    cargo tauri build
    ```
